import React from 'react'
import useStyles from './styles';
const form = () => {
    const classes = useStyles();
    return (
        <div className={classes.ready}>form</div>
    )
}

export default form